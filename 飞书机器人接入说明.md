# 飞书机器人接入说明

目标很简单：

```text
你给飞书机器人发消息或合同
  -> 机器人调用仓租采购需求整理
  -> 更新《仓库临租需求.xlsx》
  -> 回复审批邮件内容
```

现在已经有业务处理脚本，真正还差的是让飞书能访问到这台电脑上的服务。

## 你需要准备的 3 个东西

1. 飞书应用的 App ID
2. 飞书应用的 App Secret
3. 一个公网 HTTPS 地址

前两个在飞书开放平台应用后台能看到。第三个可以用 `cloudflared`、`ngrok`，或者公司服务器域名。

## 第一步：保存飞书应用信息

新建这个文件：

```text
/Users/bukeyitoukano/.warehouse-rent-feishu.env
```

内容如下：

```bash
export FEISHU_APP_ID="你的 App ID"
export FEISHU_APP_SECRET="你的 App Secret"
export FEISHU_VERIFICATION_TOKEN="你的 Verification Token，可选"
```

不要把这个文件发给别人。

## 第二步：启动本机机器人入口

在仓库目录运行：

```bash
bash 仓租采购需求整理/scripts/start_feishu_bot.sh
```

看到类似这行就说明本机服务启动了：

```text
本机地址：http://127.0.0.1:8787/feishu/events
```

## 第三步：把本机地址变成飞书能访问的地址

飞书访问不到 `127.0.0.1`，所以还需要一个公网 HTTPS 地址。

当前固定 ngrok 地址：

```text
https://duckling-enquirer-perjurer.ngrok-free.dev/feishu/events
```

启动固定 ngrok 地址：

```bash
bash 仓租采购需求整理/scripts/start_ngrok_tunnel.sh
```

## 第四步：在飞书里测试

给机器人发：

```text
新增天津（二轮）临租，面积2433.12平，2026/5/12到2026/6/30，免租0天，总租金38917.75元，帮我填表并生成审批邮件。
```

如果一切连通，机器人会先回复“已收到”，然后回复处理结果。

## 常见卡点

- 飞书后台 URL 验证失败：公网 HTTPS 地址没有正确转发到本机 `8787` 端口。
- 机器人没反应：事件订阅没有订阅 `im.message.receive_v1`，或机器人没有接收消息权限。
- 提示缺少字段：合同或消息里确实缺了需求表必填字段，需要按机器人追问补充。
- Excel 没更新：本机服务没有权限访问 `/Users/bukeyitoukano/Desktop/工作标准/仓库临租需求.xlsx`，或 Codex 执行失败。
